from setuptools import setup, find_packages
from glob import glob
import os

package_name = 'my_rob_con'

setup(
    name=package_name,
    version='0.0.0',
    packages=find_packages(exclude=['test']),
    data_files=[
        ('share/ament_index/resource_index/packages', ['resource/' + package_name]),
        ('share/' + package_name, ['package.xml']),
        ('share/' + package_name + '/launch', glob('my_rob_con/launch/*.py')),
        ('share/' + package_name + '/my_rob_con/task4', glob('my_rob_con/task4/*')),
        (os.path.join('share', package_name, 'stopnode'), glob('my_rob_con/stopnode/*.pt')),

    ],
    install_requires=['setuptools'],
    zip_safe=True,
    maintainer='fidashameer',
    maintainer_email='fidashameer06@gmail.com',
    description='Dual robot Gazebo control with gesture input',
    license='MIT',
    tests_require=['pytest'],
    entry_points={
        'console_scripts': [
            'finger_detection = my_rob_con.task4.finger_detection:main',
            'stop_node_with_cam_opening = my_rob_con.stopnode.stop_node_with_cam_opening:main',
        ],
    },
)


